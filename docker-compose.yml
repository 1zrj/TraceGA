services:
  tracega-mysql:
    image: mysql:8.0
    container_name: tracega-mysql
    ports:
      - '3306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: tracega
    volumes:
      - mysql_data:/var/lib/mysql
    profiles:
      - mysql

volumes:
  mysql_data: